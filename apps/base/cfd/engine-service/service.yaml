apiVersion: v1
kind: Service
metadata:
  name: engine-service
  namespace: cfd
spec:
  selector:
    app: engine-service
  ports:
    - name: http
      port: 80
      targetPort: 3002
      protocol: TCP
  type: ClusterIP